@model IEnumerable<PizzaApp2.Models.OrderOfPizza>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AmountPizza)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdUsers)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdPizza)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DateOrder)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AmountPizza)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdUsers)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdPizza)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateOrder)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
